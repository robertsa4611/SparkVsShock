using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Death : MonoBehaviour
{
    /*
    private void OnCollisionEnter2D(Collision2D other)
    {
        var player = other.coll.GetComponent<character>();
        if (player != null)
        {
            player.die();
        }
    }
    */
}
